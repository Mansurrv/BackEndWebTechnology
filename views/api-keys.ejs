<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>F1Tracker - API Keys Management</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;500;700&family=Racing+Sans+One&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        .api-keys-container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 100px 20px 40px;
        }
        
        .api-section {
            background: var(--card-bg);
            border-radius: var(--radius);
            padding: 30px;
            margin-bottom: 30px;
            box-shadow: var(--shadow);
        }
        
        .api-key-item {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 15px;
            background: var(--bg);
            border-radius: var(--radius);
            margin-bottom: 10px;
            border: 1px solid var(--border);
        }
        
        .api-key-info h4 {
            margin: 0 0 5px 0;
            color: var(--text);
        }
        
        .api-key-meta {
            display: flex;
            gap: 15px;
            color: var(--text-muted);
            font-size: 14px;
        }
        
        .api-key-value {
            font-family: monospace;
            background: var(--bg-light);
            padding: 8px 12px;
            border-radius: var(--radius);
            border: 1px dashed var(--border);
            margin: 10px 0;
            word-break: break-all;
        }
        
        .status-badge {
            padding: 4px 10px;
            border-radius: 20px;
            font-size: 12px;
            font-weight: 600;
        }
        
        .status-active {
            background: rgba(76, 175, 80, 0.1);
            color: #4CAF50;
        }
        
        .status-inactive {
            background: rgba(244, 67, 54, 0.1);
            color: #f44336;
        }
        
        .btn-api {
            background: linear-gradient(135deg, #9C27B0 0%, #7B1FA2 100%);
            color: white;
            border: none;
            padding: 10px 20px;
            border-radius: var(--radius);
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
        }
        
        .btn-api:hover {
            background: linear-gradient(135deg, #7B1FA2 0%, #6A1B9A 100%);
            transform: translateY(-2px);
        }
        
        .danger-actions {
            display: flex;
            gap: 10px;
        }
        
        .usage-example {
            background: var(--bg);
            padding: 20px;
            border-radius: var(--radius);
            margin-top: 20px;
            border-left: 4px solid var(--primary);
        }
        
        .usage-example code {
            display: block;
            background: var(--card-bg);
            padding: 15px;
            border-radius: var(--radius);
            margin: 10px 0;
            overflow-x: auto;
        }
    </style>
</head>
<body>
    <!-- Navigation -->
    <nav class="navbar">
        <div class="nav-container">
            <div class="logo">
                <img src="https://upload.wikimedia.org/wikipedia/commons/thumb/3/33/F1.svg/1200px-F1.svg.png" 
                    alt="F1 Logo" 
                    class="logo-img">
                <span>F1Tracker</span>
            </div>
            <ul class="nav-links">
                <li><a href="/">Home</a></li>
                <li><a href="/profile">Profile</a></li>
                <li><a href="/profile/api-keys" class="active">API Keys</a></li>
                <li><a href="/logout" class="logout-btn">Logout</a></li>
            </ul>
            <button class="mobile-menu-btn">
                <i class="fas fa-bars"></i>
            </button>
        </div>
    </nav>

    <!-- Success/Error Messages -->
    <% if (success) { %>
        <div class="alert alert-success">
            <i class="fas fa-check-circle"></i> <%= success %>
        </div>
    <% } %>

    <% if (error) { %>
        <div class="alert alert-error">
            <i class="fas fa-exclamation-circle"></i> <%= error %>
        </div>
    <% } %>

    <main class="api-keys-container">
        <!-- Header -->
        <div class="profile-header">
            <h1><i class="fas fa-key"></i> API Keys Management</h1>
            <p>Manage your API keys for programmatic access to F1 Tracker data</p>
        </div>

        <!-- Create New API Key -->
        <div class="api-section">
            <h2><i class="fas fa-plus-circle"></i> Create New API Key</h2>
            <form action="/profile/api-keys/create" method="POST" class="create-key-form">
                <div class="form-group">
                    <label for="keyName">API Key Name</label>
                    <input type="text" id="keyName" name="keyName" 
                           placeholder="e.g., 'Mobile App', 'Script', 'External Service'" 
                           required>
                    <small>Give your API key a descriptive name to remember its purpose</small>
                </div>
                <button type="submit" class="btn-api">
                    <i class="fas fa-key"></i> Generate New API Key
                </button>
            </form>
        </div>

        <!-- Existing API Keys -->
        <div class="api-section">
            <h2><i class="fas fa-list"></i> Your API Keys</h2>
            
            <% if (apiKeys.length === 0) { %>
                <div style="text-align: center; padding: 40px 20px;">
                    <i class="fas fa-key" style="font-size: 48px; color: var(--border); margin-bottom: 20px;"></i>
                    <h3>No API Keys Found</h3>
                    <p>Create your first API key to get started with programmatic access</p>
                </div>
            <% } else { %>
                <% apiKeys.forEach(key => { %>
                    <div class="api-key-item">
                        <div class="api-key-info">
                            <h4><%= key.name %></h4>
                            <div class="api-key-meta">
                                <span>Created: <%= new Date(key.createdAt).toLocaleDateString() %></span>
                                <% if (key.lastUsed) { %>
                                    <span>Last Used: <%= new Date(key.lastUsed).toLocaleDateString() %></span>
                                <% } %>
                                <span class="status-badge <%= key.isActive ? 'status-active' : 'status-inactive' %>">
                                    <%= key.isActive ? 'Active' : 'Inactive' %>
                                </span>
                            </div>
                            <div class="api-key-value">
                                <%= key.key.substring(0, 8) %>************************
                            </div>
                        </div>
                        <div class="danger-actions">
                            <% if (key.isActive) { %>
                                <form action="/profile/api-keys/revoke/<%= key._id %>" method="POST" style="display: inline;">
                                    <button type="submit" class="btn-secondary" 
                                            onclick="return confirm('Are you sure you want to revoke this API key?')">
                                        <i class="fas fa-ban"></i> Revoke
                                    </button>
                                </form>
                            <% } %>
                            <form action="/profile/api-keys/delete/<%= key._id %>" method="POST" style="display: inline;">
                                <button type="submit" class="btn-danger" 
                                        onclick="return confirm('Are you sure you want to delete this API key? This action cannot be undone.')">
                                    <i class="fas fa-trash"></i> Delete
                                </button>
                            </form>
                        </div>
                    </div>
                <% }) %>
            <% } %>
        </div>

        <!-- API Usage Instructions -->
        <div class="api-section">
            <h2><i class="fas fa-code"></i> API Usage Instructions</h2>
            
            <div class="usage-example">
                <h4>Authentication Header Method (Recommended):</h4>
                <code>curl -H "x-api-key: YOUR_API_KEY_HERE" https://yourdomain.com/api/drivers</code>
                
                <h4>Query Parameter Method:</h4>
                <code>curl https://yourdomain.com/api/drivers?apiKey=YOUR_API_KEY_HERE</code>
                
                <h4>JavaScript Fetch Example:</h4>
                <code>
const response = await fetch('https://yourdomain.com/api/drivers', {
    headers: {
        'x-api-key': 'YOUR_API_KEY_HERE'
    }
});
const data = await response.json();
                </code>
                
                <h4>Available Endpoints:</h4>
                <ul>
                    <li><strong>GET /api/drivers</strong> - Get all drivers (Public)</li>
                    <li><strong>GET /api/drivers/:id</strong> - Get specific driver (Public)</li>
                    <li><strong>POST /api/drivers</strong> - Create new driver (Protected)</li>
                    <li><strong>PUT /api/drivers/:id</strong> - Update driver (Protected)</li>
                    <li><strong>DELETE /api/drivers/:id</strong> - Delete driver (Protected)</li>
                </ul>
            </div>
        </div>
    </main>

    <script>
        // Auto-hide alerts
        setTimeout(() => {
            const alerts = document.querySelectorAll('.alert');
            alerts.forEach(alert => {
                alert.style.opacity = '0';
                setTimeout(() => alert.remove(), 300);
            });
        }, 5000);
    </script>
</body>
</html>